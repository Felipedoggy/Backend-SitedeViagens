import PacoteViagem from "./models/PacoteViagem.js";


var pacote = new PacoteViagem(5, "Pacote 2", "2022-12-30", "cidade de São Paulo", 1500.00, "Pacote 2", "8 dias", "São Paulo", 10, "imagem2.jpg");


/*pacote.criar().then(() => {
    console.log("Pacote criado com sucesso!");
}).catch((erro) => {
    console.error("Erro ao criar o pacote:", erro);
});*/

/*pacote.alterar(1).then(() => {
    console.log("Pacote alterado com sucesso!");
}).catch((erro) => {
    console.error("Erro ao alterar o pacote:", erro);
});*/

/*pacote.excluir(1).then(() => {
    console.log("Pacote excluído com sucesso!");
}).catch((erro) => {    
    console.error("Erro ao excluir o pacote:", erro);
}
);*/



/*pacote.ListarPacote(1).then((listaPacotes) => {
    for (const pacote of listaPacotes) {
        console.log(pacote.toJSON());
    }
});*/

